import{_ as s,a as t,o as i,a6 as a}from"./chunks/framework.CJDy2q7X.js";const k=JSON.parse('{"title":"Conventions","description":"","frontmatter":{"outline":false},"headers":[],"relativePath":"api/for-developers/conventions.md","filePath":"api/for-developers/conventions.md","lastUpdated":1787725411000}'),n={name:"api/for-developers/conventions.md"};function o(r,e,h,d,p,l){return i(),t("div",null,e[0]||(e[0]=[a(`<h1 id="conventions" tabindex="-1">Conventions <a class="header-anchor" href="#conventions" aria-label="Permalink to &quot;Conventions&quot;">​</a></h1><p>The rules your endpoints must follow to behave like the rest of the API — and the mistakes that cost the most time to diagnose.</p><h2 id="authentication" tabindex="-1">Authentication <a class="header-anchor" href="#authentication" aria-label="Permalink to &quot;Authentication&quot;">​</a></h2><p>You do not implement authentication. The surface you chose with <code>routePrefix</code> decides which credential the host has already enforced by the time your provider runs.</p><table tabindex="0"><thead><tr><th>Prefix</th><th>Enforced by the host</th><th>Read the caller with</th></tr></thead><tbody><tr><td><code>/api/shop</code></td><td><code>X-STOREFRONT-KEY</code> on every request</td><td><code>Auth::guard(&#39;sanctum&#39;)-&gt;user()</code> for the customer</td></tr><tr><td><code>/api/admin</code></td><td>A valid admin Integration Bearer token</td><td><code>AdminAuthHelper::resolveAdmin()</code></td></tr></tbody></table><p>A shop endpoint that needs no customer — a status check, a public lookup — simply never asks for the user. One that does needs its own check, because the storefront key alone does not identify a person:</p><div class="language-php vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">php</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$customer </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> Auth</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">::</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">guard</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;sanctum&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">user</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $customer) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    throw</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> AuthorizationException</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">__</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;productquestion::app.api.errors.unauthenticated&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">));</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h2 id="ownership-is-your-job" tabindex="-1">Ownership is your job <a class="header-anchor" href="#ownership-is-your-job" aria-label="Permalink to &quot;Ownership is your job&quot;">​</a></h2><p>The host authenticates. It does not know that <em>this</em> record belongs to <em>this</em> customer — only your module does. Every endpoint keyed by an id a client supplies must prove ownership before returning or writing anything, or you have built a way to read other people&#39;s data by guessing numbers.</p><p>Put it in one place and reuse it:</p><div class="language-php vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">php</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">class</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> QuestionContext</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> ownedQuestion</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">|</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">|</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $id)</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> object</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $id) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">            throw</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> ResourceNotFoundException</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">__</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;productquestion::app.api.errors.id-required&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">));</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        $customer </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> $this</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">customer</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">();</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        $question </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> $this</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">questionRepository</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">find</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) $id);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $question </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">||</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) $question</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">customer_id </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">!==</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) $customer</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">-&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">id) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">            throw</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> ResourceNotFoundException</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">__</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;productquestion::app.api.errors.not-found&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">));</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        }</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $question;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Return <strong>not found</strong> rather than forbidden for a record that exists but is not the caller&#39;s — a distinct 403 tells an attacker the id is real.</p><h2 id="errors" tabindex="-1">Errors <a class="header-anchor" href="#errors" aria-label="Permalink to &quot;Errors&quot;">​</a></h2><p>Throw one of the host&#39;s exceptions. Each carries its status on REST and lands in the GraphQL <code>errors</code> array, so you never format an error response yourself.</p><table tabindex="0"><thead><tr><th>Exception</th><th>REST status</th><th>Use for</th></tr></thead><tbody><tr><td><code>AuthenticationException</code></td><td>401</td><td>No credential</td></tr><tr><td><code>AuthorizationException</code></td><td>403</td><td>Authenticated, but not allowed</td></tr><tr><td><code>InvalidInputException</code></td><td>400, or the status you pass</td><td>Missing or invalid input, business rule violated</td></tr><tr><td><code>ResourceNotFoundException</code></td><td>404</td><td>Not found, or not the caller&#39;s</td></tr><tr><td><code>OperationFailedException</code></td><td>500</td><td>The operation failed for a reason the caller cannot fix</td></tr></tbody></table><p><code>InvalidInputException</code> takes an optional second argument, which is how you return <strong>422</strong> rather than 400 for something that is well-formed but not acceptable:</p><div class="language-php vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">php</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">throw</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> new</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> InvalidInputException</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">__</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;productquestion::app.api.errors.already-answered&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">), </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">422</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><p>Use 400 for a malformed or missing field and 422 for a valid payload the current state rejects — a question already answered, a window that has closed. That is the split the core endpoints use.</p><p>The package also ships a <code>ValidationException</code>, but it is <strong>mapped to 400</strong>, not 422, and it exists mainly for GraphQL, where it tags the error with a <code>validation</code> category. New code should prefer <code>InvalidInputException</code> with an explicit status; it behaves predictably on both transports.</p><p>All of these live in <code>Webkul\\BagistoApi\\Exception</code>. Each implements the interfaces the host&#39;s serializer looks for, which is how one <code>throw</code> produces the correct REST status <em>and</em> a GraphQL <code>errors[]</code> entry — a plain <code>\\Exception</code> produces a 500 on REST and leaks its message on GraphQL, so never throw one from a provider or processor.</p><p>Pass a translated message, not an English string. A key that does not exist renders as the raw key (<code>productquestion::app.api.errors.not-found</code>) in the response, which is a defect a consumer will see, so add every key to your package&#39;s <code>Resources/lang/en/app.php</code> as you use it.</p><h2 id="naming" tabindex="-1">Naming <a class="header-anchor" href="#naming" aria-label="Permalink to &quot;Naming&quot;">​</a></h2><p>Four naming rules, each enforced by a mechanism rather than a style guide. Break one and you get a specific, recognisable symptom.</p><h3 id="property-names" tabindex="-1">Property names <a class="header-anchor" href="#property-names" aria-label="Permalink to &quot;Property names&quot;">​</a></h3><p>Write <code>$product_id</code>, and clients see <code>productId</code>. The host installs a name converter that translates in both directions:</p><ul><li><strong>On the way out</strong>, it turns each property name into camelCase for the response — <code>product_id</code> becomes <code>productId</code>.</li><li><strong>On the way in</strong>, it turns a requested field name back into snake_case, so the GraphQL field <code>productId</code> is looked up as the property <code>product_id</code>.</li></ul><p>That second step is why the rule matters. When GraphQL resolves <code>productId</code>, it converts the name to <code>product_id</code> and reads <em>that</em> property off your object. Declare <code>public ?int $productId</code> and there is no <code>product_id</code> to find, so the field resolves to <strong>null</strong> — no error, no warning, just an empty value in an otherwise correct response.</p><p>REST is unaffected, because it walks the object&#39;s real properties and converts each name outward. This asymmetry is what makes the bug expensive: the REST test passes, the GraphQL query returns <code>200</code>, and only that one field is empty.</p><div class="language-php vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">php</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ?int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $product_id </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// productId — resolves on both transports</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> ?int</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> $productId </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;    </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// productId — REST only; null over GraphQL</span></span></code></pre></div><p>Single-word properties (<code>$message</code>, <code>$status</code>) are identical in both cases, which is why a half-converted class looks like it works.</p><p>If a provider or processor assigns those properties using camelCase names, add <code>use AcceptsCamelCaseWrites</code> to the class — it maps the assignment onto the snake_case property.</p><h3 id="relation-method-names" tabindex="-1">Relation method names <a class="header-anchor" href="#relation-method-names" aria-label="Permalink to &quot;Relation method names&quot;">​</a></h3><p>Same converter, same reason. A relation is read by its snake_case property name, so <code>orderItems()</code> is never found — Eloquent is asked for <code>order_items</code> and returns nothing. A to-many field then fails with <strong>&quot;Unexpected non-iterable value for to-many relation&quot;</strong>, and a to-one silently resolves null.</p><div class="language-php vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">php</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">public</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> order_items</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> HasMany</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">   // exposed as orderItems</span></span></code></pre></div><p>Single-word relations (<code>images()</code>, <code>answers()</code>) work either way, for the same reason single-word properties do.</p><h3 id="type-and-path-names" tabindex="-1">Type and path names <a class="header-anchor" href="#type-and-path-names" aria-label="Permalink to &quot;Type and path names&quot;">​</a></h3><p>The GraphQL schema is one document built from every registered resource in the store. Type names are not namespaced by module, so two resources called <code>Question</code> — yours and another vendor&#39;s — collide, and the schema build fails. That takes down <strong>every</strong> GraphQL operation on the endpoint, not just the two that clashed.</p><p>Prefix with the feature or the module: <code>ProductQuestion</code>, not <code>Question</code>. The same reasoning applies to REST: put your endpoints under a segment of your own (<code>/product-questions/...</code>) so they cannot collide with a core path added later.</p><h3 id="operation-tags" tabindex="-1">Operation tags <a class="header-anchor" href="#operation-tags" aria-label="Permalink to &quot;Operation tags&quot;">​</a></h3><p>The <code>tags:</code> in an <code>openapi</code> block decides which section your endpoint appears under in Swagger UI. Reuse one tag across a feature&#39;s operations (<code>Product Questions</code>) and they read as a set; vary the wording and they scatter through an alphabetical list.</p><h2 id="graphql-field-names-are-derived-not-chosen" tabindex="-1">GraphQL field names are derived, not chosen <a class="header-anchor" href="#graphql-field-names-are-derived-not-chosen" aria-label="Permalink to &quot;GraphQL field names are derived, not chosen&quot;">​</a></h2><p>The host composes each field name from the operation name and the resource&#39;s <code>shortName</code>:</p><table tabindex="0"><thead><tr><th>Declaration on <code>ProductQuestion</code></th><th>Field it produces</th></tr></thead><tbody><tr><td><code>Query</code></td><td><code>productQuestion</code></td></tr><tr><td><code>QueryCollection</code></td><td><code>productQuestions</code></td></tr><tr><td><code>Mutation(name: &#39;create&#39;)</code></td><td><code>createProductQuestion</code></td></tr><tr><td><code>Mutation(name: &#39;delete&#39;)</code></td><td><code>deleteProductQuestion</code></td></tr></tbody></table><p>So renaming the resource renames the field — a breaking change for clients, even though no endpoint URL moved.</p><p><strong>Never set a custom <code>name:</code> on <code>QueryCollection</code>.</strong> The auto-derived plural is the one API Platform looks for internally; a custom name makes the lookup fail with &quot;Operation collection_query not found&quot; when the schema is built. Because the schema is built as a unit, that error takes the whole GraphQL endpoint down, not only your query. Custom names on a <code>Mutation</code> are fine and expected — that is how <code>create</code>, <code>delete</code> and action mutations are named.</p><h2 id="one-broken-resource-breaks-the-whole-schema" tabindex="-1">One broken resource breaks the whole schema <a class="header-anchor" href="#one-broken-resource-breaks-the-whole-schema" aria-label="Permalink to &quot;One broken resource breaks the whole schema&quot;">​</a></h2><p>The GraphQL schema and the OpenAPI document are each compiled from <strong>every</strong> registered resource at once, on the request that first needs them. There is no per-module isolation: if your resource cannot be compiled, the build throws, and the endpoint that triggered it returns a 500 — including for core operations that have nothing to do with your module.</p><p>The practical consequence is a debugging rule. When endpoints that worked yesterday start failing right after you add or edit a resource, suspect the new resource before anything else, and confirm by commenting out its registration and clearing the cache.</p><p>Two mistakes that fail this way rather than locally:</p><ul><li><strong>A provider typed <code>mixed</code>.</strong> The return type must be <code>object|array|null</code>. <code>mixed</code> is not a type API Platform can reconcile with an operation&#39;s output, and it fatals the metadata build for every resource.</li><li><strong>A <code>Delete</code> operation with no <code>provider:</code>.</strong> API Platform has nothing to load the record with, so the route resolves to nothing and returns 404 — this one is local, but it looks like a routing problem and sends people to the wrong file.</li></ul><h2 id="performance" tabindex="-1">Performance <a class="header-anchor" href="#performance" aria-label="Permalink to &quot;Performance&quot;">​</a></h2><ul><li><strong>Do the aggregation in the query.</strong> A provider that loads rows and then fetches a relation per row is an N+1 the host cannot see or fix. Eager-load what the response needs, or use correlated subqueries for per-row counts and aggregates — the same fix the core product listing uses.</li><li><strong>Keep listing rows cheap.</strong> Fields that need an extra query or a computed lookup belong on the detail endpoint; leave them null on the listing. A field that costs one query per row costs <code>per_page</code> queries on every page load.</li><li><strong>Rebuild the caches after changing a resource</strong> — <code>php artisan bagisto-api-platform:optimize</code>. Resource metadata and the route table are both cached; a store left with the route cache cleared re-registers every API route on every request, which is slow for the whole application, not only for your endpoints.</li></ul>`,52)]))}const u=s(n,[["render",o]]);export{k as __pageData,u as default};
