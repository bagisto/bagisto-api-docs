import{_ as a,a as t,o,a6 as n}from"./chunks/framework.CJDy2q7X.js";const p=JSON.parse('{"title":"Configuration Update (GraphQL)","description":"","frontmatter":{"outline":false,"examples":[{"id":"update","title":"Update Configuration","description":"Bulk-upsert a code → value map under a slug. Returns the re-resolved values.","query":"mutation CreateAdminConfigurationUpdate($input: createAdminConfigurationUpdateInput!) {\\n  createAdminConfigurationUpdate(input: $input) {\\n    adminConfigurationUpdate {\\n      slug\\n      channel\\n      locale\\n      success\\n      message\\n      values\\n    }\\n  }\\n}\\n","variables":"{\\n  \\"input\\": {\\n    \\"slug\\": \\"sales.order_settings\\",\\n    \\"channel\\": \\"default\\",\\n    \\"locale\\": \\"en\\",\\n    \\"values\\": {\\n      \\"sales.order_settings.reorder.admin\\": \\"1\\",\\n      \\"sales.order_settings.reorder.shop\\": \\"0\\"\\n    }\\n  }\\n}\\n","response":"{\\n  \\"data\\": {\\n    \\"createAdminConfigurationUpdate\\": {\\n      \\"adminConfigurationUpdate\\": {\\n        \\"slug\\": \\"sales.order_settings\\",\\n        \\"channel\\": \\"default\\",\\n        \\"locale\\": \\"en\\",\\n        \\"success\\": true,\\n        \\"message\\": \\"Configuration updated successfully.\\",\\n        \\"values\\": {\\n          \\"sales.order_settings.reorder.admin\\": \\"1\\",\\n          \\"sales.order_settings.reorder.shop\\": \\"0\\"\\n        }\\n      }\\n    }\\n  }\\n}\\n"}]},"headers":[],"relativePath":"api/graphql-api/admin/configuration/update.md","filePath":"api/graphql-api/admin/configuration/update.md","lastUpdated":1787290888000}'),d={name:"api/graphql-api/admin/configuration/update.md"};function r(i,e,s,l,c,u){return o(),t("div",null,e[0]||(e[0]=[n('<h1 id="configuration-update-graphql" tabindex="-1">Configuration Update (GraphQL) <a class="header-anchor" href="#configuration-update-graphql" aria-label="Permalink to &quot;Configuration Update (GraphQL)&quot;">​</a></h1><p>Mutation field: <strong><code>createAdminConfigurationUpdate</code></strong>.</p><p>Bulk-upserts every entry in <code>values</code> under the given <code>slug</code>. On success the mutation returns the freshly-resolved values for that slug (the same shape as the <a href="./values.html">Values</a> query), so the client can refresh its form state without a follow-up read. See the <a href="./">Configuration overview</a> for the full flow.</p><p>The payload wraps the result in <code>adminConfigurationUpdate</code> — select <code>slug</code>, <code>success</code>, <code>message</code>, <code>channel</code>, <code>locale</code>, and <code>values</code> inside it.</p><h2 id="input" tabindex="-1">Input <a class="header-anchor" href="#input" aria-label="Permalink to &quot;Input&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Notes</th></tr></thead><tbody><tr><td><code>slug</code></td><td><code>String!</code></td><td>The slug whose subtree is being written.</td></tr><tr><td><code>channel</code></td><td><code>String</code></td><td>Channel code. Defaults to the default channel.</td></tr><tr><td><code>locale</code></td><td><code>String</code></td><td>Locale code. Defaults to the app locale.</td></tr><tr><td><code>values</code></td><td><code>Iterable!</code></td><td>Map of <code>code → value</code>. Every key MUST start with <code>slug.</code>.</td></tr></tbody></table><h2 id="validation" tabindex="-1">Validation <a class="header-anchor" href="#validation" aria-label="Permalink to &quot;Validation&quot;">​</a></h2><p>Each field&#39;s validation rules come from the schema (discovered via <a href="./menu.html">Menu</a>), resolved and enforced on the server — they are never trusted from the client. Call Menu first to know which rules apply to a given code.</p><h2 id="permission" tabindex="-1">Permission <a class="header-anchor" href="#permission" aria-label="Permalink to &quot;Permission&quot;">​</a></h2><p>Requires the admin role to carry <code>configuration</code> (or the finer-grained <code>configuration.edit</code>), or <code>permission_type = &quot;all&quot;</code>.</p><h3 id="stay-in-scope" tabindex="-1">Stay in scope <a class="header-anchor" href="#stay-in-scope" aria-label="Permalink to &quot;Stay in scope&quot;">​</a></h3><p>Every key in <code>values</code> must start with the supplied <code>slug.</code> prefix. A request with <code>slug: &quot;sales.order_settings&quot;</code> cannot write <code>catalog.inventory.stock_threshold</code> even if the fully-qualified key is supplied — the server rejects it before any write.</p><h3 id="file-image-fields-are-rest-only" tabindex="-1">File / image fields are REST-only <a class="header-anchor" href="#file-image-fields-are-rest-only" aria-label="Permalink to &quot;File / image fields are REST-only&quot;">​</a></h3><p>GraphQL has no binary transport, so <code>image</code> / <code>file</code> fields cannot be set here. Sending a non-string value for such a field is rejected; use the REST multipart endpoint instead.</p><h3 id="custom-fields-are-read-only" tabindex="-1">Custom fields are read-only <a class="header-anchor" href="#custom-fields-are-read-only" aria-label="Permalink to &quot;Custom fields are read-only&quot;">​</a></h3><p>Fields whose schema declares <code>type: &quot;custom&quot;</code> (blade-rendered in the admin) cannot be written via the API.</p><h3 id="password-fields-are-stored-plaintext" tabindex="-1">Password fields are stored plaintext <a class="header-anchor" href="#password-fields-are-stored-plaintext" aria-label="Permalink to &quot;Password fields are stored plaintext&quot;">​</a></h3><p><code>type: &quot;password&quot;</code> fields are UI-masking only; the value is stored as plaintext. This is a Bagisto core behaviour, not an API limitation.</p>',18)]))}const f=a(d,[["render",r]]);export{p as __pageData,f as default};
