import{_ as r,y as i,a as n,o as t,a4 as a,c as d,w as s,I as l,D as u,a5 as h}from"./chunks/framework.-FZumj2l.js";const P=JSON.parse('{"title":"Customers — Impersonate & GDPR (Admin)","description":"","frontmatter":{"outline":false},"headers":[],"relativePath":"api/workflows/admin/customers.md","filePath":"api/workflows/admin/customers.md","lastUpdated":1783079210000}'),c={name:"api/workflows/admin/customers.md"};function p(m,e,g,f,q,w){const o=i("Mermaid");return t(),n("div",null,[e[1]||(e[1]=a('<h1 id="customers-—-impersonate-gdpr-admin" tabindex="-1">Customers — Impersonate &amp; GDPR (Admin) <a class="header-anchor" href="#customers-—-impersonate-gdpr-admin" aria-label="Permalink to &quot;Customers — Impersonate &amp; GDPR (Admin)&quot;">​</a></h1><p>Most of the Customers menu is straight CRUD — customers, groups, review moderation, notes, addresses — covered in the <a href="/api/workflows/admin/build-an-admin-dashboard.html#4-customers">blueprint</a>. Two flows need explaining because they don&#39;t map to a simple call: <strong>impersonation</strong> (the headless version of &quot;login as customer&quot;) and <strong>GDPR</strong> (approving a request can cascade a delete, and the data download dumps every related table).</p><h2 id="agent-ask-inputs" tabindex="-1">Agent-ask inputs <a class="header-anchor" href="#agent-ask-inputs" aria-label="Permalink to &quot;Agent-ask inputs&quot;">​</a></h2><ul><li><strong>Integration token</strong> — ask the user; send as <code>Authorization: Bearer &lt;id&gt;|&lt;token&gt;</code>. Impersonate and GDPR need their permissions (customer-edit, GDPR-view/edit/delete), else the call returns 403.</li><li><strong>Server URL</strong> — ask the user for their Bagisto server&#39;s base URL (e.g. <code>https://store.example.com</code>) and prefix every endpoint path with it. Never assume localhost or a demo domain.</li></ul><h2 id="impersonate-login-as-customer" tabindex="-1">Impersonate (&quot;Login as Customer&quot;) <a class="header-anchor" href="#impersonate-login-as-customer" aria-label="Permalink to &quot;Impersonate (&quot;Login as Customer&quot;)&quot;">​</a></h2><p>The admin panel&#39;s &quot;Login as Customer&quot; does a session login and redirects to the storefront — which can&#39;t translate to a stateless API. Instead, <a href="/api/rest-api/admin/customers/impersonate/create.html">impersonate</a> issues a <strong>short-lived customer token</strong> that the client then uses to act as that customer against the Shop API.</p>',6)),(t(),d(h,null,{default:s(()=>[u(o,{id:"mermaid-27",class:"mermaid",graph:"flowchart%20LR%0A%20%20admin%5BAdmin%20calls%20impersonate%5D%20--%3E%20tok%5BCustomer%20token%20returned%3Cbr%2F%3Eexpires%20in%201%20hour%5D%0A%20%20tok%20--%3E%20shop%5BUse%20as%20Bearer%20on%20%2Fapi%2Fshop%2F*%3Cbr%2F%3Eacting%20as%20the%20customer%5D%0A"})]),fallback:s(()=>e[0]||(e[0]=[l(" Loading... ",-1)])),_:1})),e[2]||(e[2]=a('<p><code>POST /api/admin/customers/{customerId}/impersonate</code> returns the customer token plus the customer&#39;s id, email, and name, the impersonating admin&#39;s id, and an expiry. The plaintext token is shown once.</p><p>That token behaves exactly like a storefront login token: send it as <code>Authorization: Bearer &lt;token&gt;</code> alongside the storefront key on Shop API calls. It expires in one hour and carries an audit marker naming the admin who issued it. So &quot;login as customer&quot; becomes &quot;get a customer token&quot; — the correct headless translation. Requires the customer-edit permission.</p><h2 id="gdpr-data-requests" tabindex="-1">GDPR data requests <a class="header-anchor" href="#gdpr-data-requests" aria-label="Permalink to &quot;GDPR data requests&quot;">​</a></h2><p>The queue has a <a href="/api/rest-api/admin/customers/gdpr/list.html">list</a> and a <a href="/api/rest-api/admin/customers/gdpr/detail.html">detail</a> view, plus status updates and two actions. A request has a type (update or delete) and a status (pending, processing, declined, approved, or revoked).</p><h3 id="update-status" tabindex="-1">Update status <a class="header-anchor" href="#update-status" aria-label="Permalink to &quot;Update status&quot;">​</a></h3><p><a href="/api/rest-api/admin/customers/gdpr/update.html">Update</a> (a PUT) is a pure metadata change — it sets the status and an optional message, and fires the status-update email listener. Use it for moves like pending → processing or pending → declined. An invalid status is rejected with 422.</p><h3 id="process-the-destructive-action" tabindex="-1">Process (the destructive action) <a class="header-anchor" href="#process-the-destructive-action" aria-label="Permalink to &quot;Process (the destructive action)&quot;">​</a></h3><p><a href="/api/rest-api/admin/customers/gdpr/process.html">Process</a> (<code>POST /gdpr-requests/{id}/process</code>) is where a request is actually carried out:</p><ul><li>A <strong>delete</strong> request is approved <strong>and cascades a real customer delete</strong> — it runs the core delete so the GDPR module&#39;s own listeners fire.</li><li>An <strong>update</strong> request is marked approved but <strong>leaves the customer untouched</strong>. The request only carries a free-form message describing what to change; apply the actual edit through the normal customer-update endpoint afterward.</li></ul><p>Processing an already-approved or revoked request is refused with 422. Over GraphQL the input field is <code>requestId</code> (the plain <code>id</code> is shadowed on mutation inputs). Requires the GDPR-edit permission.</p><h3 id="download-data" tabindex="-1">Download data <a class="header-anchor" href="#download-data" aria-label="Permalink to &quot;Download data&quot;">​</a></h3><p><a href="/api/rest-api/admin/customers/gdpr/download-data.html">Download data</a> (<code>POST /customers/{customerId}/gdpr-download-data</code>) is ad-hoc — it isn&#39;t tied to a request. It returns the customer&#39;s full footprint (profile, addresses, orders, reviews, wishlist, notes) with the password and remember-token stripped. Requires the GDPR-view permission.</p><h3 id="delete" tabindex="-1">Delete <a class="header-anchor" href="#delete" aria-label="Permalink to &quot;Delete&quot;">​</a></h3><p><a href="/api/rest-api/admin/customers/gdpr/delete.html">Delete</a> removes the request row (a hard delete). Requires the GDPR-delete permission.</p><h2 id="gotcha-deleting-a-customer-with-active-orders" tabindex="-1">Gotcha: deleting a customer with active orders <a class="header-anchor" href="#gotcha-deleting-a-customer-with-active-orders" aria-label="Permalink to &quot;Gotcha: deleting a customer with active orders&quot;">​</a></h2><p>The plain customer delete is guarded — a customer with pending or processing orders can&#39;t be deleted, and the call returns 400. The GDPR delete-request path runs that same core delete, so the same guard applies. Resolve or cancel the customer&#39;s active orders first.</p><h2 id="graphql-notes" tabindex="-1">GraphQL notes <a class="header-anchor" href="#graphql-notes" aria-label="Permalink to &quot;GraphQL notes&quot;">​</a></h2><p>Impersonate, GDPR process, and download-data are action / result mutations — select the documented result fields (the token, the customer id, <code>requestId</code>, <code>success</code>, and so on), not a generic <code>id</code>. See the <a href="/api/workflows/admin/#graphql-the-must-know-rules">result-field rule</a>.</p><h2 id="status-codes-to-handle" tabindex="-1">Status codes to handle <a class="header-anchor" href="#status-codes-to-handle" aria-label="Permalink to &quot;Status codes to handle&quot;">​</a></h2><p>200 / 201 success · 400 delete guard (active orders) · 401 unauthenticated · 403 permission · 404 not found · 422 invalid status or an already-processed GDPR request.</p>',20))])}const v=r(c,[["render",p]]);export{P as __pageData,v as default};
